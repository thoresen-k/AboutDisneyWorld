<Window x:Class="AboutDisneyWorld.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AboutDisneyWorld"
        Title="MainWindow" Height="450" Width="800">
    
    <StackPanel Orientation="Vertical"> 
        <Border>
            <StackPanel Orientation="Vertical">
                <TextBlock Grid.Row="0"
                           Text="Lab Build Do Not Field" />

                <TextBlock Grid.Row="1"
                           Name="Version" 
                           Text="{Binding Version}"/>

                <TextBlock Grid.Row="2"
                           Name="ExtraText" 
                           Text="{Binding ExtraText}"/>
            </StackPanel>
        </Border>
        <Button Click="handleButtonClick" Width="125" Height="50">Reveal Secret Message</Button>
    </StackPanel>
</Window>
